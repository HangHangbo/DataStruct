#pragam once

  //向下调整
//array数组的开始位置
//size 数组的长度
//root  要调整的结点位置
void AdjustDown(int array[])




void ADjustUp(int array[], int size, int child)
{
	if (child == 0)
	{
	}
	int parent = (size - 1) / 2;
	while (1)
	{
		if (array[child] >array[parent])
		{
			
		}
	}
}